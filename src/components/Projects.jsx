import { ArrowUpRight, Github, Layers } from 'lucide-react'
import { usePortfolioData } from '../hooks/usePortfolioData'
import { projects as fallback } from '../data/fallback'
import SectionHeader from './SectionHeader'

function ProjectVisual({ type }) { return <div className={`project-visual ${type === 'Generative AI' ? 'ai-project' : 'app-project'}`}>{type === 'Generative AI' ? <><span className="visual-label">DOCUMENT INTELLIGENCE</span><div className="visual-conversation"><i /><i /><i /></div><div className="visual-vector">◈ &nbsp; semantic search</div></> : <><span className="visual-label">CLINIC OS</span><div className="phone"><b>Good morning</b><i /><i /><i /></div><div className="mini-panel">+ 12 appointments</div></>}</div> }
export default function Projects() { const { data } = usePortfolioData('projects', fallback); return <section id="projects" className="section shell"><SectionHeader number="04" eyebrow="Selected work" title={<>Built to solve <em>real workflows.</em></>} text="A selection of AI and full-stack applications built around useful, connected experiences." /><div className="project-list reveal">{data.map(project => <article className="project-card" key={project.id || project.title}><ProjectVisual type={project.tag} /><div className="project-info"><div className="project-kicker"><Layers size={15} /> {project.tag}</div><h3>{project.title}</h3><p>{project.description}</p><ul className="project-features">{project.features.map(feature => <li key={feature}>{feature}</li>)}</ul><div className="technology-list">{project.technologies.map(technology => <span key={technology}>{technology}</span>)}</div><div className="project-links"><a href={project.demo_url || '#'}>Live demo <ArrowUpRight size={16} /></a><a href={project.github_url || '#'}>GitHub <Github size={15} /></a></div></div></article>)}</div></section> }
