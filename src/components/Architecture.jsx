import { Database, Bot, Code2, ArrowDown } from 'lucide-react'
import SectionHeader from './SectionHeader'

const standard = [{ icon: Code2, label: 'React', detail: 'Interface' }, { icon: Code2, label: 'FastAPI', detail: 'API layer' }, { icon: Database, label: 'MySQL', detail: 'Data layer' }]
const ai = [{ icon: Code2, label: 'Frontend' }, { icon: Code2, label: 'FastAPI / Flask' }, { icon: Bot, label: 'LangChain + RAG' }, { icon: Database, label: 'Vector DB + LLM' }]
function Flow({ items }) { return <div className="architecture-flow">{items.map((item, i) => <div className="flow-part" key={item.label}><div className="flow-node"><item.icon size={18} /><span>{item.label}</span>{item.detail && <small>{item.detail}</small>}</div>{i < items.length - 1 && <ArrowDown className="flow-arrow" size={18} />}</div>)}</div> }
export default function Architecture() { return <section className="section shell"><SectionHeader number="05" eyebrow="How I build" title={<>Thoughtful layers, <em>connected systems.</em></>} /><div className="architecture-grid reveal"><article className="architecture-card"><p className="architecture-label">FULL-STACK FOUNDATION</p><h3>From interaction to information.</h3><Flow items={standard} /><span className="architecture-note">REST APIs connect every layer</span></article><article className="architecture-card ai-architecture"><p className="architecture-label">AI APPLICATION FLOW</p><h3>Grounded answers from relevant context.</h3><Flow items={ai} /><span className="architecture-note">Retrieve → enrich → respond</span></article></div></section> }
